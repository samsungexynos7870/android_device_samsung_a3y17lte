# A720SKSU5CTL2 (a7y17lte, Android 9 Pie)
#
# NOTE: This list is intentionally EMPTY since the switch to the
# open-source QCOM (QCA9377) Bluetooth stack (source-built CAF
# libbt-vendor + generic AOSP android.hardware.bluetooth@1.0 HIDL
# service, see device.mk). It previously extracted the prebuilt QTI
# Bluetooth HAL and the ANT+ extension from this dump; those blobs must
# no longer be shipped:
#
#   * two android.hardware.bluetooth@1.0::IBluetoothHci providers
#     (prebuilt -qti service + source-built service) were racing over
#     the service manager ("registering over instance of ..."),
#   * the prebuilt QTI libbt-vendor was shadowing the source-built one,
#   * and the Bluetooth stack FATAL-crashed on first enable
#     (bt_stack_manage, alarm.cc check failure).
#
# The original entries are kept below (commented out) for reference. If
# the proprietary stack is ever restored on purpose, also revert the
# open-source libbt-vendor/service packages and the matching
# ANT+/bluetooth blob_fixup() removal in extract-files.sh.

# A720SKSU5CTL2

# Bluetooth
#-system/vendor/lib/hw/android.hardware.bluetooth@1.0-impl-qti.so:vendor/lib/hw/android.hardware.bluetooth@1.0-impl-qti.so
#-system/vendor/lib64/hw/android.hardware.bluetooth@1.0-impl-qti.so:vendor/lib64/hw/android.hardware.bluetooth@1.0-impl-qti.so
#-system/lib/vendor.samsung.hardware.bluetooth@1.0.so:vendor/lib/vendor.samsung.hardware.bluetooth@1.0.so
#-system/lib64/vendor.samsung.hardware.bluetooth@1.0.so:vendor/lib64/vendor.samsung.hardware.bluetooth@1.0.so
#system/vendor/bin/hw/android.hardware.bluetooth@1.0-service-qti:vendor/bin/hw/android.hardware.bluetooth@1.0-service-qti
#system/vendor/etc/init/android.hardware.bluetooth@1.0-service-qti.rc:vendor/etc/init/android.hardware.bluetooth@1.0-service-qti.rc

# ANT+ support libraries. The HCI HAL impl, interface package and
# service are SOURCE-BUILT in the device tree (ant/ + interfaces/), and
# this list must stay compatible with them:
#
# * libantradio.so is SOURCE-BUILT from external/ant-wireless/ant_native
#   with the src/qualcomm-hidl backend (BOARD_ANT_WIRELESS_DEVICE set to
#   "qualcomm-hidl"): the app-side ANT client (AntHalService.apk /
#   Play-Store ANT apps) links against it. No prebuilt is extracted
#   anywhere (system or vendor), the source module owns the install paths.
# * com.qualcomm.qti.ant@1.0.so is extracted for the app-side binder only;
#   on the vendor side the source-built interface package provides it.
#   DO NOT also add it to PRODUCT_PACKAGES from a prebuilt module source -
#   the source-built interfaces/ant/1.0 module owns that name.
# The QTI Bluetooth stack entries stay commented out on purpose.
# com.qualcomm.qti.ant@1.0.so is NOT extracted: the interface package is
# built from source (interfaces/ant/1.0, system+vendor variants).
# libantradio.so not extracted anywhere: source-built (see above). The
# source module also owns the system/lib[64]/libantradio.so install paths,
# so extracting the prebuilt would collide with it on the target files.
#system/lib64/libantradio.so:vendor/lib64/libantradio.so
#system/lib/libantradio.so:vendor/lib/libantradio.so
#system/lib64/libantradio.so
#system/lib/libantradio.so
#-system/app/AntHalService/AntHalService.apk
