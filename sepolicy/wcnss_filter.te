# Qualcomm wcnss_filter daemon: performs the initial combo chip (QCA9377)
# HCI/UART setup on behalf of the CAF libbt-vendor before the Bluetooth
# stack attaches. Started/stopped by init on vendor.wc_transport.start_hci.
type wcnss_filter, domain;
type wcnss_filter_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(wcnss_filter)

allow wcnss_filter bluetooth_device:chr_file { ioctl open read write };

set_prop(wcnss_filter, vendor_bluetooth_prop)
